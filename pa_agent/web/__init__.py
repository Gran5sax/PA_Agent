"""PA Agent web server: a FastAPI backend serving the React SPA.

Run with ``python -m pa_agent.web`` (see ``__main__.py``). Coexists with the
PyQt6 GUI entry point (``python -m pa_agent.main``); both share the same
analysis backend via ``AppContext``.
"""
